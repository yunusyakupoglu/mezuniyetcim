@model IEnumerable<mezuniyetcim.com.Entities.tblVizyon>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_admin.cshtml";
}

<div class="col-md-6 col-sm-12">
    <div class="row">
        <div class="col-md-6">
            <div class="filled-rectangle-button">
                <a asp-action="Create">Vizyon Ekle</a>
            </div>
        </div>
    </div>
</div>
<div class="alternate-table">
    <table class="table">
        <thead>
            <tr>
                <th>
                    Vizyon Başlığı
                </th>
                <th>
                    Vizyon Açıklaması
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.vizyonTitle)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.vizyonDescription)
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.vizyonId">Düzenle</a> |
                        <a asp-action="Details" asp-route-id="@item.vizyonId">Detaylar</a> |
                        <a asp-action="Delete" asp-route-id="@item.vizyonId">Sil</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
    </div>